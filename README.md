# Haul (Web)

Vite + React + TypeScript app for Haul.

## Getting started

```bash
npm install
npm run dev
```

## Routing + auth

- Public route: `/login`
- Protected app shell: `/app/*`
- Auth is currently a simple localStorage-backed mock (any email + password works) so the protected routing flow is in place.
